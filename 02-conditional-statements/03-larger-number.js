function isLarger(a, b) {
    let largerNumber;

    if (a > b) {
        largerNumber = a;
    } else {
        largerNumber = b;
    }

    console.log(largerNumber);
}

isLarger(2, 4);
isLarger(7, 12);
isLarger(-1, -5);
